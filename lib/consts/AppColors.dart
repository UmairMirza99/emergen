import 'package:flutter/material.dart';

class AppColors{
static const blackColor = Colors.black;
static const whiteColor = Color(0xffffffff);
static const darkRedColor = Color(0xff8B0000);
static const blueColor = Colors.blue;
}